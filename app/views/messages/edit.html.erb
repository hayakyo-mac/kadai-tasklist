<h1>id: <%= @message.id %> のToDoList 編集ページ</h1>

<%= form_for(@message) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>

  <%= f.submit '投稿' %>
<% end %>

<%= link_to '一覧に戻る', messages_path %>