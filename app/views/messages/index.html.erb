<h1>ToDo List</h1>

<ul>
  <% @messages.each do |message| %>
    <li><%= link_to message.id, message %> : <%= message.period %> &gt; <%= message.title %> &gt; <%= message.content %></li>
  <% end %>
</ul>
<%= paginate @messages %>


<%= link_to '新規タスクの投稿', new_message_path %>